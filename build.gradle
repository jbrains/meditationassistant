// Global Gradle variables
// Version globals disabled due to F-Droid incompatibility
//ext {
//    versionCode = 126
//    versionName = "1.2.6"
//}

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.google.gms:google-services:1.3.0-beta1'
    }
}

tasks.whenTaskAdded { task ->
    if (task.name.equals("lint")) {
        task.enabled = false
    }
}
